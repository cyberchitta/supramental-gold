<%# Empty default — consumers shadow by placing a file with the same path
    under their own _includes/. The main site uses this slot for its CSR
    template-manager wiring. Sub-sites typically leave it empty. %>
