# Dependencies
node_modules/

# Build output
# Note: dist/styles.css is intentionally tracked — it's the compiled
# bundle consumers fetch via jsDelivr / passthrough copy.
_site/
.eleventy-cache/

# Local commit-message drafts
.commit-msg

# Working notes — the private working-notes repo, symlinked in.
# No trailing slash: this is a symlink, and git treats a symlink as a file.
_notes

# Backstop for a note written at the root by reflex. This repo is public and
# had two such strays untracked AND unignored, so the patterns earn their keep.
TODO.md
NEXT_STEPS.md
*-handoff.md
vibe-draft.md

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.local
